# Протокол обмена «модуль JN5169 ↔ хост-контроллер» (виртуальные устройства, JSON)

Модуль JN5169 — одновременно Zigbee-роутер и три виртуальных устройства
без физических выходов. Всё состояние живёт в ZCL-атрибутах, хост —
единственный исполнитель. Обмен — текстовые JSON-строки с `\n` на конце.

## 1. Транспорт

| Параметр | Значение |
|---|---|
| Интерфейс | UART0 JN5169, 115200 8N1, без RTS/CTS |
| Кадрирование | строка заканчивается `\n` (`\r` игнорируется), до 127 символов |
| Совместимость | бинарный SL-протокол `jntool` (кадры `01 ... 03`) на том же UART живёт параллельно: сброс (`0x0011`), стирание PDM (`0x0012`). Парсеры различают трафик по первому байту (`{` — JSON, `0x01` — SL) |

## 2. Эндпоинты и кластеры (профиль HA `0x0104`)

| EP | Устройство (ID) | Кластеры |
|---|---|---|
| 1 | Extended Colour Light (`0x010D`) + роутер | Basic `0000`, Identify `0003`, OnOff `0006`, Level `0008`, Colour `0300` |
| 2 | Light Sensor (`0x0106`) | Illuminance Measurement `0400` |
| 3 | Combined Interface (`0x0007`) | OnOff `0006` (play/stop), Level `0008` (volume), Multistate Output `0013` (мелодия) |

Basic/Identify — только EP1. Identify на EP2/EP3 нет.

## 3. Модуль → хост (снапшоты состояния)

```json
{"cmd":"on","onoff":1,"level":254,"r":255,"g":255,"b":255,"seq":41}
{"cmd":"ring","play":1,"volume":132,"melody":3,"seq":42}
```

Каждая строка модуля несёт монотонный `"seq"` (счётчик с перезапуском
от 1, врап на 2^32). Записи PDM иногда тормозят главный цикл на
секунды — строки могут приходить пачками и не по порядку. Хост применяет
только строки с `seq` больше последнего виденного, остальные дропает.
Подряд идущие дубли (то же состояние, неважно каким `cmd`) модуль не шлёт
вовсе; `seq` при этом не тратится. Запросные ответы (`get`, `getieee`)
приходят всегда, даже без изменений.

Коды `cmd` лампы: `on`, `off`, `toggle`, `level`, `color_xy`, `color_hs`,
`move`, `write`, `update`. Коды звонка: `ring`, `mute`, `volume`,
`melody`, `write`, `update`. Служебные: `resetting`, `erasing`.

Снапшоты идемпотентны (повтор ничего не меняет). Яркость/цвет лампы —
как раньше: цвет в пересчёте на полную яркость, хост умножает сам.

## 4. Хост → модуль (команды и состояние)

Все поля необязательны, группы независимы (можно мешать в одной строке),
неизвестные ключи игнорируются, порядок и пробелы свободные.

```json
{"onoff":1,"level":254,"r":255,"g":0,"b":0}
{"lux":12500}
{"play":1}
{"volume":132}
{"melody":3}
{"reset":1}
{"erase_pdm":1}
{"getieee":1}
```

| Ключ | Диапазон | Действие |
|---|---|---|
| `onoff` | `0` / nonzero | лампа вкл/выкл |
| `level` | `0`–`255` | яркость лампы (кламп к 254) |
| `r`, `g`, `b` | `0`–`255` | цвет лампы, только полной тройкой |
| `lux` | `0`–`65534` | датчик: raw MeasuredValue как есть, без математики |
| `play` | `0` / nonzero | звонок: `1` — играть, `0` — стоп |
| `volume` | `0`–`255` | громкость (кламп к 254) |
| `melody` | `0`–`65534` | индекс мелодии (кламп к `0xFFFE`) |
| `reset` | nonzero | ответить `{"cmd":"resetting"}` и перезагрузиться |
| `erase_pdm` | nonzero | ответить `{"cmd":"erasing"}`, стереть PDM, перезагрузиться |
| `getieee` | nonzero | ответить `{"cmd":"ieee","ieee":"00158D00030B20CD","joined":1}` (IEEE + в сети ли) |

Состояние применяется → PDM (кроме люксов: свежие каждые 5 секунд,
хранить нечего; и флага play: после ребута всегда стоп) → эхо
применённого состояния назад (`cmd:write`, у люксов эха нет).

Правило без петель: хост шлёт только при своих изменениях, не эхом.
Входящие строки от модуля — только с растущим `seq` (см. выше).

## 5. Модуль → сеть: два режима

**Лампа EP1 — классика**: движок репортов, координатор подписывается
Configure Reporting, конфиг в PDM. Дефолты: OnOff/Level/XY — min 1с,
max 3600с (change 1/1/16).

**Датчик EP2 и звонок EP3 — явные репорты**: каждое изменение сразу
уходит юникастом Report Attributes на координатора (`0x0000`),
без подписок, биндингов, слотов и PDM. Люксы — при каждом пуше хоста
(без ACK); play/mute — с ACK; volume/melody — без ACK.

## 6. Семантика OnOff звонка (EP3)

| Команда сети / строка | Атрибут | Хост | Строка хосту |
|---|---|---|---|
| Off (`0x00`) / `{"play":0}` | → 0 | стоп | `{"cmd":"mute","play":0,...}` |
| On (`0x01`) / `{"play":1}` | → 1 | играть (мелодия+громкость) | `{"cmd":"ring","play":1,...}` |
| Toggle (`0x02`) | инверсия | стоп ↔ играть | по факту |

Мелодия доиграла — хост шлёт `{"play":0}`. Смена мелодии/громкости на
лету — отдельными ключами. Связки громкости с play нет: MoveToLevel
(в т.ч. WithOnOff) на EP3 меняет только громкость, play — только
OnOff-командами и ключом `play`. Мелодия пишется в Multistate Output,
атрибут PresentValue (`0x0055`, uint16).

Дискретные команды (MoveToLevel/MoveToColor/MoveToHueSat) применяются
мгновенно, движок переходов стека при этом гасится — хост всегда видит
цель сразу. Живьём стримятся только непрерывные Move/Step/Stop
(апдейты ~1/сек до Stop).

Правило независимости (оба эндпоинта): команды уровня никогда не меняют
onoff/play (даже WithOnOff-варианты), команды OnOff никогда не меняют
уровень. Состояние выводится из самой команды, а не из атрибута
(стек откладывает его запись до конца рампы).

## 7. Цвет лампы: sRGB ↔ CIE XY

Без изменений: гамма sRGB (пороги `0.04045` / `0.0031308`, степень 2.4),
матрицы D65, XY в `0`–`65279`. Чёрный → D65 white (`20413`, `21477`).
Дефолт: выкл, `0xFE`, D65. Round-trip точен на примарных цветах
(кламп), в остальных — ±1–2 LSB.

## 8. Basic-кластер (EP1)

| Атрибут | ID | Значение |
|---|---|---|
| ManufacturerName | `0x0004` | `VLK_SW` |
| ModelIdentifier | `0x0005` | `ZESP_Router` |
| HardwareVersion | `0x0003` | `2` (DGNWG05LM), `3` (ZHWG11LM) |
| DateCode / SWBuildID | `0x0006` / `0x4000` | дата / версия сборки |

## 9. Ограничения

- Очередь UART 64 байта, за проход разбирается до 32; строки ~70 байт.
- Битые строки, переполнение 127, неизвестные ключи — молча мимо.
- Частичная RGB-тройка игнорируется.
- PDM пишется на дискретных событиях, не на шагах переходов.
- Ответы `jntool` — plain-текст, JSON-парсер хоста их пропускает.
- Без сети явные репорты тихо дропаются, атрибуты обновляются.

## 10. PDM-записи и магии

Каждая запись валидируется магией `ZR` (Zigbee Router) + буква типа +
ревизия формата. Не сошлась длина или магия — запись отбрасывается,
берётся дефолт (после смены префикса с `LR` устройство стартует чистым
и уходит в steering):

| ID | Magic | Содержимое |
|---|---|---|
| `0x0001` | `0x5A524E01` (ZR+N+rev1) | состояние узла (в сети / нет) |
| `0x0002` | `0x5A525203` (ZR+R+rev3) | конфиг репортов лампы |
| `0x0003` | `0x5A524C01` (ZR+L+rev1) | состояние лампы |
| `0x0004` | `0x5A524401` (ZR+D+rev1) | громкость + мелодия (play не хранится) |
